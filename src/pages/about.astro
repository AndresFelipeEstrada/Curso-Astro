---
import "../styles/global.css"
import Layout from '../layouts/Layout.astro'
const pageTitle = 'Sobre mi'

const identity = {
	firstName: 'Andres',
	country: 'Colombia',
	occupation: 'Software Engineer',
	hobbies: ['Video juegos', 'Fotografia', 'Leer']
}
const skills = ['HTML', 'CSS', 'Javascript', 'React', 'Astro', 'Tailwind']
const happy = true
const finished = true
// const goal = 3
const skillColor = 'navy'
const fontWeight = 'bold'
const textCase = 'uppercase'
---

<Layout title={pageTitle}>
	<h1>{pageTitle}</h1>
	<h2>... ¡y mi nuevo sitio Astro!</h2>

	<p>
		Estoy trabajando en el tutorial introductorio de Astro. Esta es la segunda página de mi sitio
		web, ¡y es la primera que he construido yo mismo!
	</p>

	<p>
		Este sitio se irá actualizando a medida que vaya completando más partes del tutorial, ¡así que
		no dejes de visitarlo para ver cómo va mi viaje!
	</p>

	<ul>
		<li>Me llamo {identity.firstName}</li>
		<li>Vivo en {identity.country} y trabajo como {identity.occupation}</li>

		{
			identity.hobbies.length >= 2 && (
				<li>
					{' '}
					Dos de mis aficiones son : {identity.hobbies[0]} y {identity.hobbies[1]}
				</li>
			)
		}
	</ul>

	<p>Mis habilidades son:</p>

	<ul>
		{skills.map((skill) => <li class="skill">{skill} </li>)}
	</ul>

	{happy && <p> Estoy feliz aprendiendo Astro </p>}
	{finished && <p>He terminado este tutorial </p>}
	<!-- {goal === 3 ? <p>Mi objetivo es terminar en 3 días.</p> : <p>Mi objetivo no son 3 días.</p>} -->
</Layout>

<style define:vars={{ skillColor, fontWeight, textCase }}>
	h1 {
		color: purple;
		font-size: 4rem;
	}
	ul {
		color: white;
	}
	.skill {
		color: var(--skillColor);
		font-weight: var(--fontWeight);
		text-transform: var(--textCase);
	}

	p {
		text-align: center;
	}
</style>
